## Registration notification email
##
## Parameters: {0} The name of the DSpace instance
##             {1} The URL of the DSpace instance
##             {2} Name:
##             {3} Email:
##             {4} Registration Date:
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: Notificação de Registro")

Um novo usuário se registrou em ${params[0]} às ${params[1]}:

Nome:                   ${params[2]}
E-mail:                 ${params[3]}
Data:                   ${params[4]}

Equipe ${config.get('dspace.name')}
