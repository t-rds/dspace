## E-mail sent with the information filled out in a feedback form.
##
## Parameters: {0} current date
##             {1} email address that the user provided
##             {2} logged in as
##             {3} page that the user was on when they selected feedback
##             {4} User-Agent HTTP Header
##             {5} Session Id
##             {6} The user's comments
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: Informações do formulário de feedback")

Comentários:

${params[6]}


Data: ${params[0]}
E-mail: ${params[1]}
Logado como: ${params[2]}
Página de referência: ${params[3]}
User Agent: ${params[4]}
Sessão: ${params[5]}

Equipe ${config.get('dspace.name')}
