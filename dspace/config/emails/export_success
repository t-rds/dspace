## Email sent to DSpace users when they successfully export an item or collection.
##
## Parameters: {0} is expanded to a special URL
##             {1} configurable time (hours) an export file will be kept
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: Exportação de item pronta para download")

A exportação do item solicitada do repositório está pronta para download.

Você pode baixar o arquivo compactado usando o seguinte link:
${params[0]}

Este arquivo ficará disponível para download por pelo menos ${params[1]} horas.

Equipe ${config.get('dspace.name')}
