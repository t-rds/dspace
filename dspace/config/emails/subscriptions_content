## E-mail sent to designated address about updates on subscribed items
##
## Parameters: {0} Collections updates
##             {1} Communities updates
#set($subject = "Assinaturas ${config.get('dspace.name')}")

Este e-mail foi enviado a partir de ${config.get('dspace.name')} com base nas preferências de assinatura escolhidas.

#if( not( "$params[0]" == "" ))
Assinaturas de Comunidades:
---------------------------
Lista de itens alterados: ${params[0]}

#end
#if( not( "$params[1]" == "" ))
Assinaturas de Coleções:
------------------------
Lista de itens alterados: ${params[1]}
#end
