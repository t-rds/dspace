## E-mail sent by the healthcheck command to whomever is mentioned as -e parameter.
##
## Parameters: {0} is the output of healthcheck command
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: Verificação de integridade do repositório")

A verificação de integridade terminou com a seguinte saída:

${params[0]}
