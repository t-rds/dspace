## Email sent to DSpace users when they successfully export an item or collection.
##
## Parameters: {0} the export error
##             {1} the URL to the feedback page
##
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: A exportação do item solicitada não foi concluída.")

A exportação do item solicitada não foi concluída devido ao seguinte motivo:
 ${params[0]}

Para obter mais informações, entre em contato com o administrador do sistema:
 ${params[1]}

Equipe ${config.get('dspace.name')}
